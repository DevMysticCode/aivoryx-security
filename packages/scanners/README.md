# Scanner Plugins

Each scanner plugin (e.g. `scanner-headers`, `scanner-tls`, `scanner-cors`) lives as its
own package under this directory, implementing the `ScannerPlugin` interface from
`@aivoryx/scanner-core`.

No scanner packages exist yet — this directory is a placeholder for Phase 2+.
